package pipe_project;

/**
 * Encapsulating an external action.
 * 
 * The invokeExternalAction function is called when the connected transition
 * is fired.
 */
public interface ExternalActionInterface {

	public void invokeExternalAction();
	
}
